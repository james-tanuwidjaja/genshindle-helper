# Keep the build context small and avoid copying host-specific files.
node_modules
dist
.git
.github
.vscode
.idea
*.log
*.md
Dockerfile
.dockerignore
docker-compose.yml

# Dev-only tooling (not needed inside the image)
eslint.config.js
.prettierrc.json
.prettierignore
