# Normalize line endings: store text as LF in the repo, check out LF on every OS.
# This stops the "LF will be replaced by CRLF" warnings on Windows.
* text=auto eol=lf

# Treat image assets as binary (no line-ending or diff munging).
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.webp binary
