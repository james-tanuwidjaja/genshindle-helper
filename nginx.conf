server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Single-page app: fall back to index.html so client-side routes work.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
